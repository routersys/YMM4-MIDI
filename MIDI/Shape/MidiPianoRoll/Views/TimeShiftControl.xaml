<UserControl x:Class="MIDI.Shape.MidiPianoRoll.Views.TimeShiftControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="42" d:DesignWidth="300"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <UserControl.Resources>

        <Style x:Key="ThemedTextBoxStyle" TargetType="TextBox">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SpinnerButtonStyle" TargetType="RepeatButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontSize" Value="8"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="contentPresenter"
                                              Focusable="False"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"
                                              RecognizesAccessKey="True"
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.56"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Bottom"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="9"/>
            <Setter Property="Margin" Value="0,-2,0,0"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
        </Style>

    </UserControl.Resources>

    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">

        <TextBox Style="{StaticResource ThemedTextBoxStyle}"
                 Text="{Binding ValueTimeSpan, Mode=OneWay, StringFormat='{}{0:hh\\:mm\\:ss\\.fff}'}" 
                 FontFamily="Courier New"
                 MinWidth="100"
                 TextAlignment="Right"
                 IsReadOnly="True"/>

        <StackPanel Margin="5,0,0,0">
            <TextBlock Style="{StaticResource LabelStyle}">H</TextBlock>
            <RepeatButton x:Name="HourUpButton" Content="▲" Click="HourUp_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
            <RepeatButton x:Name="HourDownButton" Content="▼" Click="HourDown_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
        </StackPanel>

        <StackPanel Margin="5,0,0,0">
            <TextBlock Style="{StaticResource LabelStyle}">M</TextBlock>
            <RepeatButton x:Name="MinuteUpButton" Content="▲" Click="MinuteUp_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
            <RepeatButton x:Name="MinuteDownButton" Content="▼" Click="MinuteDown_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
        </StackPanel>

        <StackPanel Margin="5,0,0,0">
            <TextBlock Style="{StaticResource LabelStyle}">S</TextBlock>
            <RepeatButton x:Name="SecondUpButton" Content="▲" Click="SecondUp_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
            <RepeatButton x:Name="SecondDownButton" Content="▼" Click="SecondDown_Click" Delay="500" Interval="100" Style="{StaticResource SpinnerButtonStyle}"/>
        </StackPanel>

        <StackPanel Margin="5,0,0,0">
            <TextBlock Style="{StaticResource LabelStyle}">ms</TextBlock>
            <RepeatButton x:Name="MsUpButton" Content="▲" Click="MsUp_Click" Delay="300" Interval="50" Style="{StaticResource SpinnerButtonStyle}"/>
            <RepeatButton x:Name="MsDownButton" Content="▼" Click="MsDown_Click" Delay="300" Interval="50" Style="{StaticResource SpinnerButtonStyle}"/>
        </StackPanel>

    </StackPanel>
</UserControl>