<UserControl x:Class="MIDI.TextCompletion.MidiMusicCompletionSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MIDI.TextCompletion"
             xmlns:viewmodels="clr-namespace:MIDI.TextCompletion.ViewModels"
             xmlns:c="clr-namespace:YukkuriMovieMaker.Controls;assembly=YukkuriMovieMaker.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=viewmodels:MidiMusicCompletionSettingsViewModel}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

        <Style TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <StackPanel>
                            <Border Background="#FF4A4A4A" 
                                    BorderBrush="#FF666666" 
                                    BorderThickness="1,1,1,0" 
                                    CornerRadius="0">
                                <ContentPresenter ContentSource="Header" 
                                                  Margin="8,4,8,4" 
                                                  TextBlock.FontWeight="Bold" 
                                                  TextBlock.Foreground="#FFE0E0E0"/>
                            </Border>
                            <Border BorderBrush="#FF666666" 
                                    BorderThickness="1,0,1,1" 
                                    CornerRadius="0"
                                    Background="Transparent">
                                <ContentPresenter Margin="10"/>
                            </Border>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TabControl">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Margin" Value="10"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Background" Value="#FF3A3A3A"/>
            <Setter Property="Foreground" Value="#FFDDDDDD"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="BorderThickness" Value="1,1,1,0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border x:Name="Border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="0">
                                <ContentPresenter ContentSource="Header"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF4A4A4A"/>
                                <Setter Property="Foreground" Value="#FFFFFFFF"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,0"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Background" Value="#FF2D2D2D"/>
                                <Setter Property="Foreground" Value="#FFAAAAAA"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="1,1,1,1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF5A5A5A"/>
                                <Setter Property="Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="0,0,5,0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Background" Value="#FF4A4A4A"/>
            <Setter Property="Foreground" Value="#FFE0E0E0"/>
            <Setter Property="BorderBrush" Value="#FF666666"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF5A5A5A"/>
                                <Setter Property="Foreground" Value="#FFFFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF3A3A3A"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF888888"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#FF333333"/>
                                <Setter Property="Foreground" Value="#FF666666"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#FF555555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconCloseButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Margin" Value="5,0,0,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Width" Value="26"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Path Data="M 0 0 L 10 10 M 10 0 L 0 10" 
                          Stroke="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" 
                          StrokeThickness="2" 
                          Stretch="Uniform" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,0,0,5"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Padding" Value="3,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Padding" Value="0,0,5,0"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <TabControl>
            <TabItem Header="基本設定">
                <Border BorderBrush="#FF666666" BorderThickness="0,1,0,0" Padding="10">
                    <StackPanel>
                        <GroupBox Header="検索設定">
                            <StackPanel>
                                <CheckBox IsChecked="{Binding Settings.EnableFuzzySearch}" Content="あいまい検索を有効にする"/>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="補完設定">
                            <StackPanel>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Label Content="最大候補数:" Grid.Column="0" VerticalAlignment="Center"/>
                                    <c:TextBoxSlider Value="{Binding Settings.MaxSuggestions}" Min="1" Max="10" StringFormat="F0" Width="150" HorizontalAlignment="Left" Grid.Column="1" Unit="個"/>
                                </Grid>

                                <CheckBox IsChecked="{Binding Settings.EnableDescriptionCompletion}" Content="補完時に用語の説明を出力する" Margin="0,5,0,0"/>
                            </StackPanel>
                        </GroupBox>

                        <TextBlock TextWrapping="Wrap" Margin="5,5,0,0">
                            MIDIや音楽に関する用語の補完、意味の検索、英訳の検索を行います。
                        </TextBlock>
                    </StackPanel>
                </Border>
            </TabItem>
            <TabItem Header="用語集管理">
                <Border BorderBrush="#FF666666" BorderThickness="0,1,0,0" Padding="10">
                    <Grid x:Name="TerminologyManagementGrid">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="LayoutStates">
                                <VisualState x:Name="Wide">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RowDef1" Storyboard.TargetProperty="Height">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <GridLength>0</GridLength>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ColDef1" Storyboard.TargetProperty="Width">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <GridLength>350</GridLength>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>

                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="(Grid.Row)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="0"/>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="(Grid.Column)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="1"/>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="Margin">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="0,0,0,0"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Compact">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="RowDef1" Storyboard.TargetProperty="Height">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <GridLength>Auto</GridLength>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ColDef1" Storyboard.TargetProperty="Width">
                                            <DiscreteObjectKeyFrame KeyTime="0">
                                                <DiscreteObjectKeyFrame.Value>
                                                    <GridLength>0</GridLength>
                                                </DiscreteObjectKeyFrame.Value>
                                            </DiscreteObjectKeyFrame>
                                        </ObjectAnimationUsingKeyFrames>

                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="(Grid.Row)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="1"/>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="(Grid.Column)">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="0"/>
                                        </ObjectAnimationUsingKeyFrames>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="TerminologyEditorPanel" Storyboard.TargetProperty="Margin">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="0,10,0,0"/>
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto" x:Name="RowDef1"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="350" x:Name="ColDef1"/>
                        </Grid.ColumnDefinitions>

                        <Grid x:Name="TerminologyListGrid" Grid.Row="0" Grid.Column="0" Margin="0,0,10,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Label Grid.Row="0" Content="用語一覧" FontWeight="Bold"/>

                            <Grid Grid.Row="1" Margin="0,5,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Grid.Column="0" Margin="0"/>
                                <Button Command="{Binding ClearSearchCommand}" Grid.Column="1" ToolTip="検索クリア" Style="{StaticResource IconCloseButton}"/>
                            </Grid>

                            <DataGrid Grid.Row="2"
                                      ItemsSource="{Binding FilteredTerms}"
                                      SelectedItem="{Binding SelectedTerm}"
                                      AutoGenerateColumns="False" 
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      SelectionMode="Single"
                                      IsReadOnly="True">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="日本語名" Binding="{Binding JapaneseName}" Width="*"/>
                                    <DataGridTextColumn Header="英語名" Binding="{Binding EnglishName}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,10,0,0">
                                <Button Content="新規追加" Command="{Binding AddTermCommand}"/>
                                <Button Content="削除" Command="{Binding DeleteTermCommand}"/>
                            </StackPanel>
                        </Grid>

                        <Border x:Name="TerminologyEditorPanel" Grid.Row="0" Grid.Column="1" BorderBrush="#FF666666" BorderThickness="1">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <Label Grid.Row="0" Content="選択項目の編集" FontWeight="Bold" Margin="10,10,10,0"/>

                                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" DataContext="{Binding SelectedTerm}" Padding="10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <Label Grid.Row="0" Grid.Column="0" Content="日本語名:"/>
                                        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding JapaneseName, UpdateSourceTrigger=PropertyChanged}" Margin="0"/>

                                        <Label Grid.Row="1" Grid.Column="0" Content="英語名:" Margin="0,5,0,0"/>
                                        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding EnglishName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,5"/>

                                        <Label Grid.Row="2" Grid.Column="0" Content="エイリアス:"/>
                                        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding AliasText, UpdateSourceTrigger=PropertyChanged}" ToolTip="カンマ(,)区切りで複数指定" Margin="0"/>

                                        <Label Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Content="説明:" Margin="0,5,0,0"/>
                                        <TextBox Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" 
                                                 Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                                                 MinHeight="100" 
                                                 Height="Auto"
                                                 TextWrapping="Wrap" 
                                                 AcceptsReturn="True"
                                                 VerticalScrollBarVisibility="Auto"
                                                 Margin="0"/>
                                    </Grid>
                                </ScrollViewer>

                                <StackPanel Grid.Row="2" Orientation="Vertical" Margin="10,10,10,10">
                                    <TextBlock Text="用語集データ管理" Margin="0,0,0,5" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="インポート" Command="{Binding ImportTermsCommand}"/>
                                        <Button Content="エクスポート" Command="{Binding ExportTermsCommand}"/>
                                        <Button Content="リセット" Command="{Binding ResetTermsCommand}" ToolTip="用語集を初期状態に戻します"/>
                                    </StackPanel>
                                </StackPanel>

                                <TextBlock Grid.Row="1"
                                           Text="用語集リスト上で項目を選択すると編集できます。"
                                           Visibility="{Binding IsTermNotSelected, Converter={StaticResource BooleanToVisibilityConverter}}"
                                           TextWrapping="Wrap"
                                           Foreground="Gray"
                                           Margin="15"
                                           IsHitTestVisible="False"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                    </Grid>
                </Border>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>