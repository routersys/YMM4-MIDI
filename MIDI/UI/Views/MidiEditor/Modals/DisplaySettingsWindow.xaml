<Window x:Class="MIDI.UI.Views.MidiEditor.Modals.DisplaySettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MIDI.UI.ViewModels.MidiEditor.Modals"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=local:DisplaySettingsViewModel}"
        Title="表示設定"
        Width="300"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}"
        Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
        ResizeMode="NoResize"
        Topmost="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Resources/MidiEditorResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <GroupBox Header="グリッド" Grid.Row="0" Margin="5">
            <ComboBox ItemsSource="{Binding GridOptions}" SelectedItem="{Binding SelectedGridOption}" />
        </GroupBox>

        <GroupBox Header="時間表示の間隔 (秒)" Grid.Row="1" Margin="5">
            <ComboBox ItemsSource="{Binding TimeRulerIntervalOptions}" SelectedItem="{Binding SelectedTimeRulerInterval}" />
        </GroupBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="キャンセル" IsCancel="True" Style="{StaticResource RichButtonStyle}" Margin="0,0,5,0"/>
            <Button Content="適用" IsDefault="True" Click="ApplyButton_Click" Style="{StaticResource RichButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>