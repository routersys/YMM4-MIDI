<UserControl x:Class="MIDI.UI.Views.MidiEditor.Controls.LoudnessMeter"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MIDI.UI.Views.MidiEditor.Controls"
             xmlns:converters="clr-namespace:MIDI.UI.Views.MidiEditor.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200"
             Background="#1E1E1E">
    <UserControl.Resources>
        <converters:LufsToPercentageConverter x:Key="LufsToPercentageConverter"/>
        <converters:HeightPercentageConverter x:Key="HeightPercentageConverter"/>
    </UserControl.Resources>
    <Viewbox Stretch="Uniform" Margin="10">
        <Grid Width="150" Height="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Loudness (LUFS)" Foreground="#CCCCCC" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,5"/>

            <Border Grid.Row="1" Background="#252526" BorderBrush="#3F3F46" BorderThickness="1" Margin="0,5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid Margin="5" x:Name="MeterGrid">
                        <Rectangle VerticalAlignment="Bottom">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="#4A90E2" Opacity="0.7"/>
                            </Rectangle.Fill>
                            <Rectangle.Height>
                                <MultiBinding Converter="{StaticResource HeightPercentageConverter}">
                                    <Binding Path="MomentaryLoudness" Converter="{StaticResource LufsToPercentageConverter}"/>
                                    <Binding ElementName="MeterGrid" Path="ActualHeight"/>
                                </MultiBinding>
                            </Rectangle.Height>
                        </Rectangle>
                        <Rectangle VerticalAlignment="Bottom" Margin="10,0">
                            <Rectangle.Fill>
                                <SolidColorBrush Color="#77ACF3"/>
                            </Rectangle.Fill>
                            <Rectangle.Height>
                                <MultiBinding Converter="{StaticResource HeightPercentageConverter}">
                                    <Binding Path="ShortTermLoudness" Converter="{StaticResource LufsToPercentageConverter}"/>
                                    <Binding ElementName="MeterGrid" Path="ActualHeight"/>
                                </MultiBinding>
                            </Rectangle.Height>
                        </Rectangle>
                        <Border BorderBrush="Yellow" BorderThickness="0,2,0,0" VerticalAlignment="Bottom" 
                                SnapsToDevicePixels="True" Margin="2,0">
                            <Border.Height>
                                <MultiBinding Converter="{StaticResource HeightPercentageConverter}">
                                    <Binding Path="IntegratedLoudness" Converter="{StaticResource LufsToPercentageConverter}"/>
                                    <Binding ElementName="MeterGrid" Path="ActualHeight"/>
                                </MultiBinding>
                            </Border.Height>
                        </Border>
                        <Border BorderBrush="#E53935" BorderThickness="0,1,0,0" VerticalAlignment="Bottom" 
                                SnapsToDevicePixels="True" Margin="2,0">
                            <Border.Height>
                                <MultiBinding Converter="{StaticResource HeightPercentageConverter}">
                                    <Binding ConverterParameter="-23" Source="-23" Converter="{StaticResource LufsToPercentageConverter}"/>
                                    <Binding ElementName="MeterGrid" Path="ActualHeight"/>
                                </MultiBinding>
                            </Border.Height>
                        </Border>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Stretch" Margin="0,5,5,5" HorizontalAlignment="Right">
                        <TextBlock Text="-5" Foreground="#CCCCCC" FontSize="8"/>
                        <TextBlock Text="-14" Foreground="#CCCCCC" FontSize="8" Margin="0,38,0,0"/>
                        <TextBlock Text="-23" Foreground="#E53935" FontSize="8" Margin="0,38,0,0"/>
                        <TextBlock Text="-32" Foreground="#CCCCCC" FontSize="8" Margin="0,38,0,0"/>
                        <TextBlock Text="-41" Foreground="#CCCCCC" FontSize="8" Margin="0,38,0,0"/>
                        <TextBlock Text="-50" Foreground="#CCCCCC" FontSize="8" Margin="0,38,0,0"/>
                        <TextBlock Text="-70" Foreground="#CCCCCC" FontSize="8" Margin="0,42,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="2" Margin="5,0,5,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="M" Foreground="#4A90E2" FontSize="10" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding MomentaryLoudness, StringFormat={}{0:F1}}" Foreground="#CCCCCC" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="S" Foreground="#77ACF3" FontSize="10" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding ShortTermLoudness, StringFormat={}{0:F1}}" Foreground="#CCCCCC" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <TextBlock Text="I" Foreground="Yellow" FontSize="10" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding IntegratedLoudness, StringFormat={}{0:F1}}" Foreground="#CCCCCC" FontSize="10" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>