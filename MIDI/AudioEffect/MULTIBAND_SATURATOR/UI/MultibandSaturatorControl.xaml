<UserControl x:Class="MIDI.AudioEffect.MULTIBAND_SATURATOR.UI.MultibandSaturatorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="clr-namespace:MIDI.AudioEffect.MULTIBAND_SATURATOR.UI"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=ui:MultibandSaturatorViewModel}"
             Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
             Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
             Height="380">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="Margin" Value="0,0,2,0"/>
        </Style>

        <Style x:Key="MeterBar" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
            <Setter Property="Orientation" Value="Vertical"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Minimum" Value="-60"/>
            <Setter Property="Maximum" Value="0"/>
            <Setter Property="BorderBrush" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
            <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
        </Style>

        <Style TargetType="Slider" BasedOn="{StaticResource {x:Type Slider}}"/>
        <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}"/>

        <Style x:Key="CircularSliderThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Grid>
                            <Ellipse x:Name="ThumbEllipse"
                                     Fill="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
                                     Stroke="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                     StrokeThickness="1"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbEllipse" Property="Fill" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter TargetName="ThumbEllipse" Property="Stroke" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbEllipse" Property="Fill" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                                <Setter TargetName="ThumbEllipse" Property="Stroke" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="VerticalSliderStyle" TargetType="{x:Type Slider}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Orientation" Value="Vertical"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Slider}">
                        <Grid>
                            <Border x:Name="TrackBorder"
                                    Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}"
                                    BorderBrush="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"
                                    BorderThickness="1"
                                    Width="4"
                                    CornerRadius="2"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="False">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.DecreaseLarge}">
                                        <RepeatButton.Style>
                                            <Style TargetType="RepeatButton">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="RepeatButton">
                                                            <Border Background="{TemplateBinding Background}"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </RepeatButton.Style>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static Slider.IncreaseLarge}">
                                        <RepeatButton.Style>
                                            <Style TargetType="RepeatButton">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="RepeatButton">
                                                            <Border Background="{TemplateBinding Background}"/>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </RepeatButton.Style>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource CircularSliderThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <CubicEase x:Key="EaseOut" EasingMode="EaseOut"/>
        <SineEase x:Key="SineInOut" EasingMode="EaseInOut"/>

        <Storyboard x:Key="TitleAnimation">
            <DoubleAnimation Storyboard.TargetName="TitleM" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.1"/>
            <DoubleAnimation Storyboard.TargetName="TitleM" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.1" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleB" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.15"/>
            <DoubleAnimation Storyboard.TargetName="TitleB" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.15" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleS" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.2"/>
            <DoubleAnimation Storyboard.TargetName="TitleS" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.2" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleA1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.25"/>
            <DoubleAnimation Storyboard.TargetName="TitleA1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.25" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleT1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.3"/>
            <DoubleAnimation Storyboard.TargetName="TitleT1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.3" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleU" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.35"/>
            <DoubleAnimation Storyboard.TargetName="TitleU" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.35" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleR1" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.4"/>
            <DoubleAnimation Storyboard.TargetName="TitleR1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.4" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleA2" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.45"/>
            <DoubleAnimation Storyboard.TargetName="TitleA2" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.45" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleT2" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.5"/>
            <DoubleAnimation Storyboard.TargetName="TitleT2" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.5" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleO" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.55"/>
            <DoubleAnimation Storyboard.TargetName="TitleO" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.55" EasingFunction="{StaticResource EaseOut}"/>

            <DoubleAnimation Storyboard.TargetName="TitleR2" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5" BeginTime="0:0:0.6"/>
            <DoubleAnimation Storyboard.TargetName="TitleR2" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="-10" To="0" Duration="0:0:0.5" BeginTime="0:0:0.6" EasingFunction="{StaticResource EaseOut}"/>
        </Storyboard>

        <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever">
            <DoubleAnimation Storyboard.TargetName="PulseGlow" Storyboard.TargetProperty="Opacity" From="0.2" To="1.0" Duration="0:0:1.5" AutoReverse="True" EasingFunction="{StaticResource SineInOut}"/>
            <DoubleAnimation Storyboard.TargetName="PulseGlow" Storyboard.TargetProperty="Width" From="70" To="120" Duration="0:0:1.5" AutoReverse="True" EasingFunction="{StaticResource SineInOut}"/>

            <DoubleAnimation Storyboard.TargetName="PulseCore" Storyboard.TargetProperty="Opacity" From="0.5" To="1.0" Duration="0:0:0.15" AutoReverse="True" BeginTime="0:0:1.5" RepeatBehavior="3x"/>
        </Storyboard>
    </UserControl.Resources>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderBrush="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" BorderThickness="0,0,0,1" Margin="0,0,0,5" Padding="2">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="2,0" VerticalAlignment="Center">
                    <TextBlock Text="M" FontSize="18" FontWeight="Bold" x:Name="TitleM" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="B" FontSize="18" FontWeight="Bold" x:Name="TitleB" Opacity="0" Margin="0,0,10,0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>

                    <TextBlock Text="S" FontSize="18" FontWeight="Bold" x:Name="TitleS" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="A" FontSize="18" FontWeight="Bold" x:Name="TitleA1" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="T" FontSize="18" FontWeight="Bold" x:Name="TitleT1" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="U" FontSize="18" FontWeight="Bold" x:Name="TitleU" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="R" FontSize="18" FontWeight="Bold" x:Name="TitleR1" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="A" FontSize="18" FontWeight="Bold" x:Name="TitleA2" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="T" FontSize="18" FontWeight="Bold" x:Name="TitleT2" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="O" FontSize="18" FontWeight="Bold" x:Name="TitleO" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                    <TextBlock Text="R" FontSize="18" FontWeight="Bold" x:Name="TitleR2" Opacity="0">
                        <TextBlock.RenderTransform>
                            <TranslateTransform Y="-10"/>
                        </TextBlock.RenderTransform></TextBlock>
                </StackPanel>

                <Grid Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Width="130" Height="20">
                    <Ellipse x:Name="PulseGlow" Fill="#FFFF4444" HorizontalAlignment="Right" VerticalAlignment="Center" Width="80" Height="4" Opacity="0.3">
                        <Ellipse.Effect>
                            <BlurEffect Radius="12"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse x:Name="PulseCore" Fill="#FFFF8888" HorizontalAlignment="Right" VerticalAlignment="Center" Width="60" Height="2" Opacity="0.8"/>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="プリセット:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Width="100" Margin="0,0,5,0"/>
                <ComboBox ItemsSource="{Binding Presets}" SelectedItem="{Binding SelectedPreset}" DisplayMemberPath="Name" Width="200"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="IN" HorizontalAlignment="Center" FontSize="10" Margin="0,0,0,2"/>
                <Grid Grid.Row="1" Width="20" HorizontalAlignment="Center">
                    <ProgressBar Style="{StaticResource MeterBar}" Value="{Binding InputLevel}" VerticalAlignment="Stretch"/>
                </Grid>
            </Grid>

            <Border Grid.Column="1" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1" Margin="2" Padding="5" Background="#11000000">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="LOW" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF5555DD"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel Margin="5">
                            <TextBlock Text="Drive" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="0" Maximum="100" Value="{Binding EffectItem.LowDrive}" x:Name="SlLowD" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="LowDTextBlock" Text="{Binding ElementName=SlLowD, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="LowD" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="LowDTextBox" Text="{Binding ElementName=SlLowD, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="LowD" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Level" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="-24" Maximum="12" Value="{Binding EffectItem.LowLevel}" x:Name="SlLowL" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="LowLTextBlock" Text="{Binding ElementName=SlLowL, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="LowL" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="LowLTextBox" Text="{Binding ElementName=SlLowL, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="LowL" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <Grid Width="10" Margin="5,0">
                            <ProgressBar Style="{StaticResource MeterBar}" Value="{Binding LowMeter}" VerticalAlignment="Stretch" Foreground="#FF5555DD"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Split Freq" FontSize="10" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding ElementName=SlFreqL, Path=Value, StringFormat={}{0:F0} Hz}" HorizontalAlignment="Center"/>
                        <Slider Minimum="20" Maximum="1000" Value="{Binding EffectItem.FreqLowMid}" x:Name="SlFreqL" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="2" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1" Margin="2" Padding="5" Background="#11000000">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="MID" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FF55DD55"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel Margin="5">
                            <TextBlock Text="Drive" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="0" Maximum="100" Value="{Binding EffectItem.MidDrive}" x:Name="SlMidD" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="MidDTextBlock" Text="{Binding ElementName=SlMidD, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="MidD" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="MidDTextBox" Text="{Binding ElementName=SlMidD, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="MidD" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Level" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="-24" Maximum="12" Value="{Binding EffectItem.MidLevel}" x:Name="SlMidL" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="MidLTextBlock" Text="{Binding ElementName=SlMidL, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="MidL" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="MidLTextBox" Text="{Binding ElementName=SlMidL, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="MidL" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <Grid Width="10" Margin="5,0">
                            <ProgressBar Style="{StaticResource MeterBar}" Value="{Binding MidMeter}" VerticalAlignment="Stretch" Foreground="#FF55DD55"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Column="3" BorderBrush="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" BorderThickness="1" Margin="2" Padding="5" Background="#11000000">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="HIGH" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#FFDD5555"/>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <StackPanel Margin="5">
                            <TextBlock Text="Drive" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="0" Maximum="100" Value="{Binding EffectItem.HighDrive}" x:Name="SlHighD" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="HighDTextBlock" Text="{Binding ElementName=SlHighD, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="HighD" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="HighDTextBox" Text="{Binding ElementName=SlHighD, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="HighD" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <StackPanel Margin="5">
                            <TextBlock Text="Level" HorizontalAlignment="Center" FontSize="10"/>
                            <Slider Style="{StaticResource VerticalSliderStyle}" Height="160" Minimum="-24" Maximum="12" Value="{Binding EffectItem.HighLevel}" x:Name="SlHighL" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                            <TextBlock x:Name="HighLTextBlock" Text="{Binding ElementName=SlHighL, Path=Value, StringFormat={}{0:F0}}" HorizontalAlignment="Center" Tag="HighL" MouseLeftButtonDown="ValueTextBlock_MouseLeftButtonDown"/>
                            <TextBox x:Name="HighLTextBox" Text="{Binding ElementName=SlHighL, Path=Value, UpdateSourceTrigger=Explicit}" Visibility="Collapsed" Tag="HighL" LostFocus="ValueTextBox_LostFocus" KeyDown="ValueTextBox_KeyDown" PreviewTextInput="ValueTextBox_PreviewTextInput" Width="30"/>
                        </StackPanel>
                        <Grid Width="10" Margin="5,0">
                            <ProgressBar Style="{StaticResource MeterBar}" Value="{Binding HighMeter}" VerticalAlignment="Stretch" Foreground="#FFDD5555"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Grid.Row="2">
                        <TextBlock Text="Split Freq" FontSize="10" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding ElementName=SlFreqH, Path=Value, StringFormat={}{0:F0} Hz}" HorizontalAlignment="Center"/>
                        <Slider Minimum="1000" Maximum="10000" Value="{Binding EffectItem.FreqMidHigh}" x:Name="SlFreqH" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="4" Margin="10,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5" VerticalAlignment="Center">
                    <TextBlock Text="Mix" HorizontalAlignment="Center" FontSize="10"/>
                    <Slider Style="{StaticResource VerticalSliderStyle}" Height="100" Minimum="0" Maximum="100" Value="{Binding EffectItem.MasterMix}" x:Name="SlMix" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                    <TextBlock Text="{Binding ElementName=SlMix, Path=Value, StringFormat={}{0:F0}%}" HorizontalAlignment="Center"/>

                    <TextBlock Text="Gain" HorizontalAlignment="Center" Margin="0,10,0,0" FontSize="10"/>
                    <Slider Style="{StaticResource VerticalSliderStyle}" Height="80" Minimum="-20" Maximum="20" Value="{Binding EffectItem.MasterGain}" x:Name="SlGain" Thumb.DragStarted="Slider_BeginEdit" Thumb.DragCompleted="Slider_EndEdit"/>
                    <TextBlock Text="{Binding ElementName=SlGain, Path=Value, StringFormat={}{0:F0}dB}" HorizontalAlignment="Center"/>
                </StackPanel>

                <Grid Grid.Column="1" Width="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="OUT" HorizontalAlignment="Center" FontSize="10" Margin="0,0,0,2"/>
                    <Grid Grid.Row="1" Width="20" HorizontalAlignment="Center">
                        <ProgressBar Style="{StaticResource MeterBar}" Value="{Binding OutputLevel}" VerticalAlignment="Stretch"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard Storyboard="{StaticResource TitleAnimation}"/>
            <BeginStoryboard Storyboard="{StaticResource PulseAnimation}"/>
        </EventTrigger>
    </UserControl.Triggers>
</UserControl>